mutation UPDATE_STATUS($statusid: Int!, $title: String!, $color: String!)
{
    result: updateStatus(statusid: $statusid, title: $title, color: $color)
    {
        status
        {
            statusid
            title
            color
            created
        }
    }
}
