mutation CREATE_STATUS($title: String!, $color: String!)
{
    result: createStatus(title: $title, color: $color)
    {
        status
        {
            statusid
            title
            color
            created
        }
    }
}
