query GET_WAREHOUSES_LIST($active: Boolean, $incoming: Boolean, $outgoing: Boolean) {
    warehouses (active: $active, incoming: $incoming, outgoing: $outgoing) {
        edges {
            node {
                value: warehouseid
                label: title
            }
        }
    }
}
