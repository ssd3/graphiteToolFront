query GET_WAREHOUSES_LIST($active: Boolean, $inField: Boolean, $outField: Boolean) {
    warehouses (active: $active, inField: $inField, out: $outField) {
        edges {
            node {
                value: warehouseid
                label: title
            }
        }
    }
}
